node_modules
dist
*.log
.DS_Store

# Local build-time endpoints and server credentials, at the root and in
# infra/ (.env.example files are the committed templates).
.env
.env.local

# Playwright MCP scratch output (page snapshots, console logs, screenshots)
.playwright-mcp/
# Generated by xcodegen from apple/project.yml (run apple/bootstrap.sh)
apple/Word.xcodeproj/
apple/Word/Info.plist

# Swift Package Manager build products (packages can live below apple/Packages/).
.build/

# Per-developer signing settings (see apple/Local.xcconfig.example)
apple/Local.xcconfig
# Generated by XcodeGen from project.yml
apple/Word/Word.entitlements
# Release artefacts (apple/tools/release.sh)
apple/.release/
