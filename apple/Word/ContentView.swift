import SwiftUI

/// The native app. The phase-1 proof-of-life screen this replaced lives on in
/// git history; phase 2 drives WordCore through the unified board gesture
/// layer, the editing loop, the paced session lifecycle, and the tutorial.
struct ContentView: View {
    var body: some View {
        RootView()
    }
}

#Preview {
    ContentView()
}
