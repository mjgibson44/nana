# Copy to Local.env (gitignored) once you've made an App Store Connect API key.
# Both ids are on the Users and Access → Integrations page; the issuer id sits
# above the key list, the key id beside your key.
ASC_KEY_ID=
ASC_ISSUER_ID=
