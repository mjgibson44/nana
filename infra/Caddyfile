# HTTPS for the PeerJS broker. Caddy gets and renews the Let's Encrypt
# certificate on its own — the only requirement is that $MP_HOST already
# resolves to this machine when it first starts.
{
	email {$ACME_EMAIL}
}

{$MP_HOST} {
	reverse_proxy peerjs:9000
}
